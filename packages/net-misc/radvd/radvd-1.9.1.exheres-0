# Copyright MMXII
#     Mikael Voss <mikael+exherbo@illdefined.org>
# Distributed under the terms of the GNU General Public License v2

require systemd-service

SUMMARY="IPv6 Router Advertisement Daemon"
HOMEPAGE="http://www.litech.org/${PN}/"
DOWNLOADS="http://www.litech.org/${PN}/dist/${PNV}.tar.gz"

REMOTE_IDS="freecode:${PN}"

LICENCES="BSD-4"
SLOT="0"
PLATFORMS="~amd64 ~x86"

DEPENDENCIES="
	build+run:
		user/radvd
		dev-libs/libdaemon"

src_install() {
	default

	install_systemd_files
}
